const Router = require('express').Router();

Router.get('/api/product/getData', (req, res) => {
	//code
});

Router.get('/api/product/addToCart', (req, res) => {
	//code
});

module.exports = Router;
